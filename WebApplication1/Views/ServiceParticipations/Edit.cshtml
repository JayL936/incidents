@model WebApplication1.Models.ServiceParticipation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div >
        <h4>ServiceParticipation</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.IncidentId)

        @Html.HiddenFor(model => model.RoleId)
        @Html.HiddenFor(model => model.RoleName)
        @Html.HiddenFor(model => model.confirmed)

        <div class="form-group">
            @Html.LabelFor(model => model.AboutParticipation, htmlAttributes: new { @class = "control-label col-md-2" })
            <div >
                @Html.TextAreaFor(model => model.AboutParticipation, htmlAttributes: new { @class = "form-control", @style = "width: 100%; max-width: 100%;", @rows = 15 })
                @Html.ValidationMessageFor(model => model.AboutParticipation, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div >
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

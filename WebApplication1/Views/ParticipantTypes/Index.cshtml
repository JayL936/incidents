@model IEnumerable<WebApplication1.Models.ParticipantType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.pTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pTypeID })
        </td>
    </tr>
}

</table>
